                    Python Terms Quiz (Form 5)

Name: _____________________________

Date: ____________

1. What is the definition of 'Lambda'?
    A. A reserved memory location to store values.
    B. A small anonymous function.
    C. Used to perform different actions based on different conditions.
    D. A way to form new classes using classes that have already been defined.

2. What is the definition of 'Function'?
    A. A way to form new classes using classes that have already been defined.
    B. A small anonymous function.
    C. A concise way to create lists.
    D. A block of code which only runs when it is called.

3. What is the definition of 'Loop'?
    A. An instance of a class.
    B. Used for iterating over a sequence.
    C. A way to form new classes using classes that have already been defined.
    D. A code template for creating objects.

4. What is the definition of 'List'?
    A. A collection which is unordered, changeable, and indexed. No duplicate members.
    B. A way of organizing related modules.
    C. A collection which is ordered and changeable. Allows duplicate members.
    D. An event that can disrupt the normal flow of a program.

5. What is the definition of 'List Comprehension'?
    A. A concise way to create lists.
    B. A reserved memory location to store values.
    C. A collection which is ordered and unchangeable. Allows duplicate members.
    D. A block of code which only runs when it is called.

6. What is the definition of 'Polymorphism'?
    A. A function that takes another function and extends its behavior without explicitly modifying it.
    B. A sequence of characters.
    C. An instance of a class.
    D. The ability to present the same interface for different underlying forms.

7. What is the definition of 'Dictionary'?
    A. A collection which is ordered and changeable. Allows duplicate members.
    B. A collection which is unordered, changeable, and indexed. No duplicate members.
    C. A way to form new classes using classes that have already been defined.
    D. A code template for creating objects.

8. What is the definition of 'Package'?
    A. A file containing a set of functions you want to include in your application.
    B. A collection which is unordered and unindexed. No duplicate members.
    C. A way of organizing related modules.
    D. A function that takes another function and extends its behavior without explicitly modifying it.

9. What is the definition of 'Exception'?
    A. An event that can disrupt the normal flow of a program.
    B. A function that returns an iterator that yields a series of values.
    C. A small anonymous function.
    D. Used for iterating over a sequence.

10. What is the definition of 'Object'?
    A. The ability to present the same interface for different underlying forms.
    B. A reserved memory location to store values.
    C. A collection which is unordered and unindexed. No duplicate members.
    D. An instance of a class.

11. What is the definition of 'Decorator'?
    A. A collection which is ordered and changeable. Allows duplicate members.
    B. An event that can disrupt the normal flow of a program.
    C. Used for iterating over a sequence.
    D. A function that takes another function and extends its behavior without explicitly modifying it.

12. What is the definition of 'String'?
    A. A function that returns an iterator that yields a series of values.
    B. Used to perform different actions based on different conditions.
    C. A sequence of characters.
    D. A reserved memory location to store values.

13. What is the definition of 'Inheritance'?
    A. A way to form new classes using classes that have already been defined.
    B. A function that returns an iterator that yields a series of values.
    C. A sequence of characters.
    D. A collection which is unordered and unindexed. No duplicate members.

14. What is the definition of 'Class'?
    A. The ability to present the same interface for different underlying forms.
    B. An event that can disrupt the normal flow of a program.
    C. A code template for creating objects.
    D. Used to perform different actions based on different conditions.

15. What is the definition of 'Tuple'?
    A. A collection which is ordered and changeable. Allows duplicate members.
    B. A code template for creating objects.
    C. A block of code which only runs when it is called.
    D. A collection which is ordered and unchangeable. Allows duplicate members.

16. What is the definition of 'Module'?
    A. An event that can disrupt the normal flow of a program.
    B. A file containing a set of functions you want to include in your application.
    C. A function that returns an iterator that yields a series of values.
    D. A small anonymous function.

17. What is the definition of 'Conditional'?
    A. A collection which is unordered and unindexed. No duplicate members.
    B. A code template for creating objects.
    C. Used to perform different actions based on different conditions.
    D. A function that returns an iterator that yields a series of values.

18. What is the definition of 'Generator'?
    A. A collection which is unordered and unindexed. No duplicate members.
    B. A function that returns an iterator that yields a series of values.
    C. A concise way to create lists.
    D. A collection which is unordered, changeable, and indexed. No duplicate members.

19. What is the definition of 'Variable'?
    A. A collection which is ordered and unchangeable. Allows duplicate members.
    B. An instance of a class.
    C. A reserved memory location to store values.
    D. A collection which is unordered, changeable, and indexed. No duplicate members.

20. What is the definition of 'Set'?
    A. Used to perform different actions based on different conditions.
    B. A collection which is unordered and unindexed. No duplicate members.
    C. A reserved memory location to store values.
    D. A collection which is ordered and changeable. Allows duplicate members.

